# Frontend (Wine-Q)

## Setup

1. Copy `.env.example` to `.env`.
2. Set `VITE_API_BASE_URL`:
   - Local: `http://localhost:8000`
   - Render: your deployed FastAPI URL
3. Install and run:
   - `npm install`
   - `npm run dev`

## Build

- `npm run build`
- Output directory: `dist/`
